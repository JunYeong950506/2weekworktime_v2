import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.twoweekworktime.webview',
  appName: '2Week Worktime',
  webDir: 'dist',
};

export default config;
